{
  "name": "buildover",
  "version": "0.0.2",
  "description": "BuildOver CLI for managing build overlays",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "bin": {
    "buildover": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "buildover",
    "cli",
    "build",
    "overlay"
  ],
  "dependencies": {
    "buildover-server": "workspace:*",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "dotenv": "^17.2.4"
  },
  "devDependencies": {
    "tsup": "^8.0.1",
    "typescript": "^5.7.2"
  }
}
